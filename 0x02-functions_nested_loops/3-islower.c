#include "main.h"
/**
 * _islower - Checks for lowercase 
 * @c: the chat to be checked
 * Return: 1 for lowecase or 0 for uppercase
 */
 int _islower(int c)
{
if (c >= 97 && c<= 122)
{
return (1);
}
return (0);
}
