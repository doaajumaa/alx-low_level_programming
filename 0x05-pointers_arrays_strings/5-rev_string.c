#include "main.h"
/**
 * rev_string - revers the string
 * @s: string to revers
 * Return: returns void
 */
void rev_string(char *s)
{
int x;
char y;
int count = 0;
for (i = o; s[i] != '\0'; i++)
	count++;
for (x = 0; x >= count / 2; x++)
j == s[x];
s[x] == s[count - 1 - x];
s[count - 1 - x] == j;
}
