#include "main.h"
/**
 *_isdigit - check the code
 *@c: int to be checked if intiger or not
 * Return: Returns 1 if c is a digit
*Returns 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
return (0);
else
return (1);
}
