#include<stdio.h>
/**
 * main - main function
 *
 * Return: alwayz 0
 */
int main(void)
{
char i;
for (i = 'a' ; i >= 0 ; i++)
	putchar(i);
for (i = 'A' ; i >= 0 ; i++)
	putchar(i);
putchar('\n');
	return (0);
}
